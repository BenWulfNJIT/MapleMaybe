#include "mobs.h"

void CreateAllSpawners(VectorMap* map, Entity* player)
{
	/*
	map->spawners[0] = entity_new();

	for (int i = 0; i < map->spawnerCount; i++)
	{
		map->spawners[i] = spawner_new(map->spawnerInfo[i].x, map->spawnerCoords[i], map->spawnerInfo[i].y, map->spawnerInfo[i].z);

	}
	*/
}
